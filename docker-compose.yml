version: '3'
services:
  websockets:
    build: .
    container_name: websockets
    restart: always
    tty: true
    ports:
      - 8765:8765
    volumes:
      - .:/app
    entrypoint: /app/entrypoint.sh
    networks:
      - proxynet
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  proxynet:
    name: otpay_network